# Smoke-only host-port isolation. The Rust harness layers this after the
# canonical installation and showcase files, while retaining all service,
# policy, health, and storage configuration from those sources of truth.
services:
  surrealdb:
    ports: !reset []
  rustfs:
    ports: !reset []
  artifact-service:
    ports: !reset []
