services:
  cloudflared:
    image: cloudflare/cloudflared:2026.2.0
    command:
      - tunnel
      - --no-autoupdate
      - run
      - --token
      - "${CLOUDFLARED_TUNNEL_TOKEN:?set CLOUDFLARED_TUNNEL_TOKEN}"
    depends_on:
      - mcp-gateway
      - media-mcp
    profiles:
      - tunnel
