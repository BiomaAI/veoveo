apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bioma-veoveo
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: bioma
  sources:
    - repoURL: charts-registry.argocd.svc.cluster.local/charts
      chart: veoveo
      targetRevision: 0.1.0-628ee1df1027
      helm:
        releaseName: veoveo
        valueFiles:
          - $configuration/examples/bioma/values.yaml
          - $configuration/examples/bioma/k3d-values.yaml
          - $configuration/examples/bioma/images.lock.yaml
    - repoURL: https://github.com/BiomaAI/veoveo.git
      targetRevision: c9fb6f1e0bcddc2ee3c6d56cb96d7a1e3bf36b3c
      ref: configuration
  destination:
    server: https://kubernetes.default.svc
    namespace: veoveo
  syncPolicy:
    automated:
      enabled: true
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=true
