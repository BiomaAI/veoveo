apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: bioma-uav-sim
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "10"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: bioma
  sources:
    - repoURL: charts-registry.argocd.svc.cluster.local/charts
      chart: uav-sim
      targetRevision: 0.1.0-08d766cff999
      helm:
        releaseName: uav-sim
        valueFiles:
          - $configuration/examples/bioma/uav-sim-values.yaml
          - $configuration/examples/bioma/images.lock.yaml
    - repoURL: https://github.com/BiomaAI/veoveo.git
      targetRevision: 6efe90a6b9874d2f6b5a1898dc27acc5a9bff770
      ref: configuration
  destination:
    server: https://kubernetes.default.svc
    namespace: veoveo
  syncPolicy:
    automated:
      enabled: true
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=true
