# Simulation Qualification Types

Read `DESIGN.md` and the parent runtime design before changing these types.
Keep process execution in the owning runtime or tooling. Preserve hardware checks
and immutable image correlation. Use deployment artifact types for shared IDs.
Run `cargo test -p veoveo-simulation-contract` through the evidence recorder.
