/* Generated by cargo xtask release client-types. Edit the owning Rust contract. */

/**
 * Veoveo profile id exposed under `/mcp/{profile}`.
 *
 * This interface was referenced by `ConsoleBootstrap`'s JSON-Schema
 * via the `definition` "GatewayProfileId".
 */
export type GatewayProfileId = string;
/**
 * Stable authenticated user or service-principal identity.
 *
 * This interface was referenced by `ConsoleBootstrap`'s JSON-Schema
 * via the `definition` "PrincipalId".
 */
export type PrincipalId = string;
/**
 * Tenant, organization, or customer boundary identifier.
 *
 * This interface was referenced by `ConsoleBootstrap`'s JSON-Schema
 * via the `definition` "TenantId".
 */
export type TenantId = string;
/**
 * How the current actor obtained authority to perform the work.
 *
 * This interface was referenced by `ConsoleBootstrap`'s JSON-Schema
 * via the `definition` "InvocationMode".
 */
export type InvocationMode = "direct" | "delegated" | "automated";
/**
 * A member's authority inside one Work Context.
 *
 * Ordering is intentional. It lets an enforcement point compare the current
 * membership with the minimum level required by an operation.
 *
 * This interface was referenced by `ConsoleBootstrap`'s JSON-Schema
 * via the `definition` "WorkContextMembershipLevel".
 */
export type WorkContextMembershipLevel = "viewer" | "contributor" | "custodian" | "owner";
/**
 * Tenant-local boundary that governs related work and every output it produces.
 *
 * This interface was referenced by `ConsoleBootstrap`'s JSON-Schema
 * via the `definition` "WorkContextId".
 */
export type WorkContextId = string;

export interface ConsoleBootstrap {
  canReadInstallation: boolean;
  installation: ConsoleInstallation;
  profile: GatewayProfileId;
  session: ConsoleSession;
}
/**
 * This interface was referenced by `ConsoleBootstrap`'s JSON-Schema
 * via the `definition` "ConsoleInstallation".
 */
export interface ConsoleInstallation {
  accentColor?: string | null;
  generatedAt: string;
  logo?: string | null;
  name: string;
  offlineMode: boolean;
  productLabel: string;
  version: string;
}
/**
 * This interface was referenced by `ConsoleBootstrap`'s JSON-Schema
 * via the `definition` "ConsoleSession".
 */
export interface ConsoleSession {
  actorId: PrincipalId;
  availableTenants: ConsoleTenant[];
  displayName: string;
  invocationMode: InvocationMode;
  membership: WorkContextMembershipLevel;
  principalId: PrincipalId;
  tenantId: TenantId;
  tenantName: string;
  workContext: WorkContextId;
  workContextTitle: string;
}
/**
 * This interface was referenced by `ConsoleBootstrap`'s JSON-Schema
 * via the `definition` "ConsoleTenant".
 */
export interface ConsoleTenant {
  id: TenantId;
  name: string;
}
